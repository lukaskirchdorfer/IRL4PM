categorical_features:
  - level
  - region
numerical_features:
  - value
  - processing_time
  - time_since_arrival # if this is set, there must be a column called arrival or enabled_time in the log
  - time_until_due_date # if this is set, there must be a column called due_date in the log
temporal_columns:
  - arrival
  - due_date
  - start_timestamp
  - end_timestamp